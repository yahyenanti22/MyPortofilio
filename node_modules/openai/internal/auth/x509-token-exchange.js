"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.exchangeX509Token = exchangeX509Token;
const error_1 = require("../../core/error.js");
const values_1 = require("../utils/values.js");
const x509_transport_capability_1 = require("./x509-transport-capability.js");
const x509_transport_registry_1 = require("./x509-transport-registry.js");
const _x509_transport_state_1 = require('./x509-transport-state.js');
const TOKEN_EXCHANGE_URL = new URL('https://mtls.auth.openai.com/oauth/token');
const TOKEN_EXCHANGE_GRANT = 'urn:ietf:params:oauth:grant-type:token-exchange';
const X509_SUBJECT_TOKEN = 'urn:openai:params:oauth:token-type:x509';
const ACCESS_TOKEN_TYPE = 'urn:ietf:params:oauth:token-type:access_token';
const SAFE_ACCESS_TOKEN = /^[A-Za-z0-9._~+/-]+=*$/u;
const SAFE_OAUTH_ERRORS = new Set(['invalid_grant', 'invalid_subject_token', 'token_exchange_server_error']);
const SAFE_RESPONSE_HEADERS = ['retry-after', 'retry-after-ms', 'x-should-retry', 'x-request-id'];
const MAX_TOKEN_EXCHANGE_DURATION_MS = 5000;
function transientConnectionError(message, failure) {
    const error = new error_1.APIConnectionError({ message });
    if ((0, x509_transport_registry_1.isRetryableX509TransportFailure)(failure)) {
        (0, _x509_transport_state_1.markTransientX509ConnectionError)(error);
    }
    return error;
}
async function cancelReader(reader, reason) {
    try {
        await reader.cancel(reason);
    }
    catch {
        // Response retirement is best-effort and must never replace the sanitized exchange result.
    }
}
function cancelResponseBody(response) {
    if (!response.body) {
        return;
    }
    const reader = response.body.getReader();
    try {
        void cancelReader(reader);
    }
    finally {
        reader.releaseLock();
    }
}
async function readResponseBody(response, signal) {
    if (!response.body) {
        throw new error_1.OpenAIError('X.509 workload identity token exchange returned invalid JSON.');
    }
    const reader = response.body.getReader();
    const chunks = [];
    let totalBytes = 0;
    const cancel = () => {
        void cancelReader(reader, signal?.reason);
    };
    signal?.addEventListener('abort', cancel, { once: true });
    try {
        while (true) {
            signal?.throwIfAborted();
            // oxlint-disable-next-line no-await-in-loop -- Token-response chunks must be consumed in wire order.
            const chunk = await reader.read();
            signal?.throwIfAborted();
            if (chunk.done) {
                break;
            }
            chunks.push(chunk.value);
            totalBytes += chunk.value.byteLength;
        }
    }
    catch (error) {
        cancel();
        if (signal?.aborted) {
            signal.throwIfAborted();
        }
        throw transientConnectionError('X.509 workload identity token response could not be read.', error);
    }
    finally {
        signal?.removeEventListener('abort', cancel);
        reader.releaseLock();
    }
    const bytes = new Uint8Array(totalBytes);
    let offset = 0;
    for (const chunk of chunks) {
        bytes.set(chunk, offset);
        offset += chunk.byteLength;
    }
    try {
        return JSON.parse(new TextDecoder('utf-8', { fatal: true }).decode(bytes));
    }
    catch {
        throw new error_1.OpenAIError('X.509 workload identity token exchange returned invalid JSON.');
    }
}
function validateTokenResponse(value) {
    if (typeof value !== 'object' || value === null || Array.isArray(value)) {
        throw new error_1.OpenAIError('X.509 workload identity token exchange returned an invalid token response.');
    }
    const response = value;
    if (!(0, values_1.hasOwn)(response, 'access_token') ||
        typeof response['access_token'] !== 'string' ||
        !SAFE_ACCESS_TOKEN.test(response['access_token'])) {
        throw new error_1.OpenAIError('X.509 workload identity token exchange returned an invalid access token.');
    }
    if (!(0, values_1.hasOwn)(response, 'token_type') || !(0, values_1.hasOwn)(response, 'issued_token_type')) {
        throw new error_1.OpenAIError('X.509 workload identity token exchange returned an invalid token type.');
    }
    const tokenType = response['token_type'];
    if (typeof tokenType !== 'string' ||
        tokenType.toLowerCase() !== 'bearer' ||
        response['issued_token_type'] !== ACCESS_TOKEN_TYPE) {
        throw new error_1.OpenAIError('X.509 workload identity token exchange returned an invalid token type.');
    }
    if (!(0, values_1.hasOwn)(response, 'expires_in')) {
        throw new error_1.OpenAIError('X.509 workload identity token exchange returned an invalid token lifetime.');
    }
    const expiresIn = response['expires_in'];
    if (typeof expiresIn !== 'number' || !Number.isFinite(expiresIn) || expiresIn <= 0 || expiresIn > 3600) {
        throw new error_1.OpenAIError('X.509 workload identity token exchange returned an invalid token lifetime.');
    }
    return { accessToken: response['access_token'], expiresIn };
}
function safeResponseHeaders(response, includeRetryHints = false) {
    const headers = new Headers();
    for (const name of SAFE_RESPONSE_HEADERS) {
        if (name !== 'x-request-id' && !includeRetryHints) {
            continue;
        }
        const value = response.headers.get(name);
        if (value !== null) {
            headers.set(name, value);
        }
    }
    return headers;
}
function readOAuthErrorCode(value) {
    if (typeof value === 'string') {
        return value;
    }
    if (typeof value === 'object' && value !== null && (0, values_1.hasOwn)(value, 'code')) {
        const code = Object.getOwnPropertyDescriptor(value, 'code')?.value;
        return typeof code === 'string' ? code : undefined;
    }
    return undefined;
}
async function oauthError(response, signal, callerSignal) {
    let errorCode;
    try {
        const parsed = await readResponseBody(response, signal);
        if (typeof parsed === 'object' && parsed !== null && (0, values_1.hasOwn)(parsed, 'error')) {
            const code = readOAuthErrorCode(Object.getOwnPropertyDescriptor(parsed, 'error')?.value);
            if (code !== undefined && SAFE_OAUTH_ERRORS.has(code)) {
                errorCode = { error: code };
            }
        }
    }
    catch {
        callerSignal?.throwIfAborted();
        if (signal?.aborted &&
            !(signal.reason instanceof error_1.APIConnectionTimeoutError && (0, _x509_transport_state_1.isTransientX509ConnectionError)(signal.reason))) {
            signal.throwIfAborted();
        }
    }
    const { status } = response;
    if (status !== 400 && status !== 401 && status !== 403) {
        throw new error_1.OpenAIError('X.509 workload identity received an invalid OAuth error status.');
    }
    const headers = safeResponseHeaders(response);
    const error = new error_1.OAuthError(status, errorCode, headers);
    (0, _x509_transport_state_1.rememberX509OAuthError)(error, { status, error: errorCode, headers });
    return error;
}
/** Exchanges one enrolled client certificate for a validated OpenAI workload access token. */
async function exchangeX509Token(options) {
    const { signal: callerSignal } = options;
    callerSignal?.throwIfAborted();
    const { identityProviderId, serviceAccountId, transport } = options;
    if (typeof identityProviderId !== 'string' ||
        identityProviderId.trim().length === 0 ||
        typeof serviceAccountId !== 'string' ||
        serviceAccountId.trim().length === 0) {
        throw new error_1.OpenAIError('X.509 workload identity requires nonempty provider and service-account identifiers.');
    }
    const timeoutController = new AbortController();
    const signal = callerSignal
        ? AbortSignal.any([callerSignal, timeoutController.signal])
        : timeoutController.signal;
    const timeout = setTimeout(() => {
        const error = new error_1.APIConnectionTimeoutError({
            message: 'X.509 workload identity token exchange timed out.',
        });
        (0, _x509_transport_state_1.markTransientX509ConnectionError)(error);
        timeoutController.abort(error);
    }, MAX_TOKEN_EXCHANGE_DURATION_MS);
    timeout.unref();
    try {
        const body = JSON.stringify({
            grant_type: TOKEN_EXCHANGE_GRANT,
            subject_token_type: X509_SUBJECT_TOKEN,
            identity_provider_id: identityProviderId,
            service_account_id: serviceAccountId,
        });
        let response;
        try {
            response = await (0, x509_transport_capability_1.sendX509Request)(transport, TOKEN_EXCHANGE_URL, {
                method: 'POST',
                headers: { 'Content-Type': 'application/json' },
                body,
                redirect: 'manual',
                signal,
            });
        }
        catch (error) {
            signal.throwIfAborted();
            throw transientConnectionError('X.509 workload identity token exchange connection failed.', error);
        }
        signal.throwIfAborted();
        if (response.ok) {
            try {
                return validateTokenResponse(await readResponseBody(response, signal));
            }
            catch (error) {
                signal.throwIfAborted();
                if (error instanceof error_1.APIConnectionError) {
                    throw error;
                }
                if (error instanceof error_1.OpenAIError) {
                    throw error_1.APIError.generate(response.status, undefined, error.message, safeResponseHeaders(response));
                }
                throw error;
            }
        }
        if (response.status === 400 || response.status === 401 || response.status === 403) {
            throw await oauthError(response, signal, callerSignal);
        }
        cancelResponseBody(response);
        const retryableStatus = response.status === 408 || response.status === 409 || response.status === 429 || response.status >= 500;
        const headers = safeResponseHeaders(response, retryableStatus);
        const error = error_1.APIError.generate(response.status, undefined, 'X.509 workload identity token exchange failed.', headers);
        if (retryableStatus && headers.get('x-should-retry') !== 'false') {
            (0, _x509_transport_state_1.markRetryableX509IssuerError)(error);
        }
        throw error;
    }
    finally {
        clearTimeout(timeout);
    }
}
//# sourceMappingURL=x509-token-exchange.js.map