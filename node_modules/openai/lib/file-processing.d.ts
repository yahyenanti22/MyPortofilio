import type { FileObject, Files } from "../resources/files.js";
/**
 * Waits for file processing through the resource's retrieve method. The timeout
 * is checked after each subsequent retrieval, preserving the existing behavior
 * for an initially terminal file and for a terminal response received too late.
 *
 * @internal
 */
export declare function waitForFileProcessing(resource: Pick<Files, 'retrieve'>, id: string, pollInterval: number, maxWait: number): Promise<FileObject>;
//# sourceMappingURL=file-processing.d.ts.map